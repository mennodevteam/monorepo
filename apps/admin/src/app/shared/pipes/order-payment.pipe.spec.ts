import { OrderPaymentPipe } from './order-payment.pipe';

describe('OrderPaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderPaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
