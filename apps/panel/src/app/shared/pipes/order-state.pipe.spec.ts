import { OrderStatePipe } from './order-state.pipe';

describe('OrderStatePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderStatePipe();
    expect(pipe).toBeTruthy();
  });
});
